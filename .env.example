NEXT_PUBLIC_API_BASE=https://api.example.com
NEXT_PUBLIC_WS_BASE=wss://api.example.com

# Gemini API Key (required for image analysis)
GEMINI_API_KEY=your-gemini-api-key

# Google Cloud TTS credentials (choose one):
# Option 1 (recommended for Render): Paste full JSON content of service account key
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key":"..."}
# Option 2: Path to credentials file (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
